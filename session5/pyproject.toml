[project]
name = "action-item-cli"
version = "0.1.0"
description = "CLI for managing action items with validation"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
