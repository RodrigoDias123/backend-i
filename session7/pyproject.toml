[project]
name = "meeting-cli"
version = "0.1.0"
description = "CLI for managing meetings with JSON persistence"
requires-python = ">=3.8"
dependencies = [
    "click>=8.0.0",
    "pydantic>=2.0.0",
]

[tool.uv]
